import {
  StyleSheet
} from 'react-native';

export default StyleSheet.create({
    sliderContainer: {
        padding: 15,
    },

    sliderPoint: {
        width: 16,
        height: 16,
        borderWidth: 2,
        borderColor: "rgb(83,45,62)",
        position: 'absolute',
        top: 11,
        borderRadius: 14,
    },

    colorsImage: {
        width: 285,
        height: 8,
    },
});
