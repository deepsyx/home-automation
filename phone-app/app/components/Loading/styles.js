import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
    progress: {
        width: 150,
    },

    spinner: {
        width: 100,
        height: 100,
    },

    container: {
        alignItems:'center',
        justifyContent:'center',
        flexDirection:'row',
        flex: 1
    },
});
